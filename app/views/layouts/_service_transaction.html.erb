  <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Transaction <span class="caret"></span></a>
                   <ul class="dropdown-menu">
                         <% if user_signed_in? %>
                            <% if current_user.role.name == "admin" || current_user.role.name == "staff" %>
                            <li> <%= link_to "New Service Transaction", new_service_transaction_path  %></li>
                            <% else %>
                            <% end %>
                            <li><%= link_to "View Service Transactions", service_transactions_path  %></li>
                            <% end %>
                      </ul>
                  </li>
                  